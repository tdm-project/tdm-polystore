language: python

python: 3.6

cache: pip

services: docker

before_install: pip install 'flake8>=3.6'

install:
  - make images

before_script: flake8 . && true

script:
 - make start
 - sleep 5 #  Sleep a few seconds to try to make sure HDFS is ready
 - make run-tests
 - make stop
